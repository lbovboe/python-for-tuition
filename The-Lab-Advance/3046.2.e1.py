"""
Learning Objectives:
- Familiarise with forward()/backward()/left()/right()
"""
import turtle

# Create a graphical window
myscreen = turtle.Screen()

# Create a pen
mypen = turtle.Turtle()

# Draw L shape
# Does not have to be the following commands,
# it is acceptable to draw the L in any other way
mypen.backward(150)
mypen.left(90)
mypen.forward(150)